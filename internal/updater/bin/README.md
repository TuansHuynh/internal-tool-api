# Stub file - replaced by real updater binary at CI build time.
# This file is intentionally committed so that `go build .` works for developers
# without needing to build the updater first.
# The CI workflow builds the real updater.exe before running `wails build`.
